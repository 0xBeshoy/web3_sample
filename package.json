{
  "name": "web3_sample",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "copy": "copyfiles -f -V build/contracts/*.json ./client/abi/",
    "dev": "lite-server --baseDir='client'",
    "runAll": "npm run copy && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "lite-server": "^2.6.1"
  },
  "dependencies": {
    "@truffle/hdwallet-provider": "^1.7.0"
  }
}
